[project]
name = "stx-vc-pool"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/stx-backed-crowdfunding/stx-vc-pool/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.stx-vc-pool]
path = "contracts/stx-vc-pool.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
